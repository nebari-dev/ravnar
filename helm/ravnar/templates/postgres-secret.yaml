{{- if and .Values.postgres.enabled (not .Values.postgres.auth.existingSecret) -}}
{{- $component := "postgres" }}
{{- $name := include "ravnar.component-name" (dict "top" . "component" $component) }}
{{- $username := "" }}
{{- $password := "" }}
{{- if $existingSecret := lookup "v1" "Secret" .Release.Namespace $name }}
  {{- $username = (index $existingSecret.data "username" | b64dec) }}
  {{- $password = (index $existingSecret.data "password" | b64dec) }}
{{- else }}
  {{- $username = "huginn" }}
  {{- $password = randAlphaNum 24 }}
{{- end }}
{{- $database := "ravnar" }}
{{- $host := include "ravnar.component-name" (dict "top" . "component" $component) }}
{{- $port := "5432" }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $name }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ravnar.labels" (dict "top" . "component" $component) | nindent 4 }}
  annotations:
    "helm.sh/resource-policy": "keep"
type: Opaque
stringData:
  username: {{ $username }}
  password: {{ $password }}
  host: {{ $host }}
  port: {{ $port }}
  database: {{ $database }}
{{- end -}}
